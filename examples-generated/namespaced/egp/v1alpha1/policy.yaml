apiVersion: egp.vault.m.upbound.io/v1alpha1
kind: Policy
metadata:
  annotations:
    meta.upbound.io/example-id: egp/v1alpha1/policy
  labels:
    testing.upbound.io/example-name: allow-all
  name: allow-all
  namespace: upbound-system
spec:
  forProvider:
    enforcementLevel: soft-mandatory
    name: allow-all
    paths:
    - '*'
    policy: |
      main = rule {
        true
      }
