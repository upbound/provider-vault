apiVersion: azure.vault.m.upbound.io/v1alpha1
kind: SecretBackend
metadata:
  annotations:
    meta.upbound.io/example-id: azure/v1alpha1/secretbackend
  labels:
    testing.upbound.io/example-name: azure
  name: azure
  namespace: upbound-system
spec:
  forProvider:
    clientIdSecretRef:
      key: example-key
      name: example-secret
    identityTokenAudience: <TOKEN_AUDIENCE>
    identityTokenTtl: <TOKEN_TTL>
    rotationSchedule: 0 * * * SAT
    rotationWindow: 3600
    subscriptionIdSecretRef:
      key: example-key
      name: example-secret
    tenantIdSecretRef:
      key: example-key
      name: example-secret
