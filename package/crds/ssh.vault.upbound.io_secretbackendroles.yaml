---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: secretbackendroles.ssh.vault.upbound.io
spec:
  group: ssh.vault.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - vault
    kind: SecretBackendRole
    listKind: SecretBackendRoleList
    plural: secretbackendroles
    singular: secretbackendrole
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecretBackendRole is the Schema for the SecretBackendRoles API.
          Managing roles in an SSH secret backend in Vault
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecretBackendRoleSpec defines the desired state of SecretBackendRole
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  algorithmSigner:
                    description: 'When supplied, this value specifies a signing algorithm
                      for the key. Possible values: ssh-rsa, rsa-sha2-256, rsa-sha2-512.'
                    type: string
                  allowBareDomains:
                    description: Specifies if host certificates that are requested
                      are allowed to use the base domains listed in allowed_domains.
                    type: boolean
                  allowHostCertificates:
                    description: Specifies if certificates are allowed to be signed
                      for use as a 'host'.
                    type: boolean
                  allowSubdomains:
                    description: Specifies if host certificates that are requested
                      are allowed to be subdomains of those listed in allowed_domains.
                    type: boolean
                  allowUserCertificates:
                    description: Specifies if certificates are allowed to be signed
                      for use as a 'user'.
                    type: boolean
                  allowUserKeyIds:
                    description: Specifies if users can override the key ID for a
                      signed certificate with the key_id field.
                    type: boolean
                  allowedCriticalOptions:
                    description: Specifies a comma-separated list of critical options
                      that certificates can have when signed.
                    type: string
                  allowedDomains:
                    description: The list of domains for which a client can request
                      a host certificate.
                    type: string
                  allowedDomainsTemplate:
                    description: |-
                      Specifies if allowed_domains can be declared using
                      identity template policies. Non-templated domains are also permitted.
                    type: boolean
                  allowedExtensions:
                    description: Specifies a comma-separated list of extensions that
                      certificates can have when signed.
                    type: string
                  allowedUserKeyConfig:
                    description: |-
                      Set of configuration blocks to define allowed
                      user key configuration, like key type and their lengths. Can be specified multiple times.
                      See
                      Set of allowed public key types and their relevant configuration
                    items:
                      properties:
                        lengths:
                          description: |-
                            A list of allowed key lengths as integers.
                            For key types that do not support setting the length a value of [0] should be used.
                            Setting multiple lengths is only supported on Vault 1.10+. For prior releases length
                            must be set to a single element list.
                            List of allowed key lengths, vault-1.10 and above
                          items:
                            type: number
                          type: array
                        type:
                          description: |-
                            The SSH public key type.
                            Supported key types are:
                            rsa, ecdsa, ec, dsa, ed25519, ssh-rsa, ssh-dss, ssh-ed25519,
                            ecdsa-sha2-nistp256, ecdsa-sha2-nistp384, ecdsa-sha2-nistp521
                            Key type, choices:
                            rsa, ecdsa, ec, dsa, ed25519, ssh-rsa, ssh-dss, ssh-ed25519, ecdsa-sha2-nistp256, ecdsa-sha2-nistp384, ecdsa-sha2-nistp521
                          type: string
                      type: object
                    type: array
                  allowedUsers:
                    description: Specifies a comma-separated list of usernames that
                      are to be allowed, only if certain usernames are to be allowed.
                    type: string
                  allowedUsersTemplate:
                    description: Specifies if allowed_users can be declared using
                      identity template policies. Non-templated users are also permitted.
                    type: boolean
                  backend:
                    description: The path where the SSH secret backend is mounted.
                    type: string
                  backendRef:
                    description: Reference to a Mount in vault to populate backend.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  backendSelector:
                    description: Selector for a Mount in vault to populate backend.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  cidrList:
                    description: The comma-separated string of CIDR blocks for which
                      this role is applicable.
                    type: string
                  defaultCriticalOptions:
                    additionalProperties:
                      type: string
                    description: Specifies a map of critical options that certificates
                      have when signed.
                    type: object
                    x-kubernetes-map-type: granular
                  defaultExtensions:
                    additionalProperties:
                      type: string
                    description: Specifies a map of extensions that certificates have
                      when signed.
                    type: object
                    x-kubernetes-map-type: granular
                  defaultUser:
                    description: Specifies the default username for which a credential
                      will be generated.
                    type: string
                  defaultUserTemplate:
                    description: If set, default_users can be specified using identity
                      template values. A non-templated user is also permitted.
                    type: boolean
                  keyIdFormat:
                    description: Specifies a custom format for the key id of a signed
                      certificate.
                    type: string
                  keyType:
                    description: Specifies the type of credentials generated by this
                      role. This can be either otp, dynamic or ca.
                    type: string
                  maxTtl:
                    description: Specifies the maximum Time To Live value.
                    type: string
                  name:
                    description: |-
                      Specifies the name of the role to create.
                      Unique name for the role.
                    type: string
                  namespace:
                    description: |-
                      The namespace to provision the resource in.
                      The value should not contain leading or trailing forward slashes.
                      The namespace is always relative to the provider's configured namespace.
                      Available only for Vault Enterprise.
                      Target namespace. (requires Enterprise)
                    type: string
                  notBeforeDuration:
                    description: |-
                      Specifies the duration by which to backdate the ValidAfter property.
                      Uses duration format strings.
                      Specifies the duration by which to backdate the ValidAfter property. Uses duration format strings.
                    type: string
                  ttl:
                    description: Specifies the Time To Live value.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  algorithmSigner:
                    description: 'When supplied, this value specifies a signing algorithm
                      for the key. Possible values: ssh-rsa, rsa-sha2-256, rsa-sha2-512.'
                    type: string
                  allowBareDomains:
                    description: Specifies if host certificates that are requested
                      are allowed to use the base domains listed in allowed_domains.
                    type: boolean
                  allowHostCertificates:
                    description: Specifies if certificates are allowed to be signed
                      for use as a 'host'.
                    type: boolean
                  allowSubdomains:
                    description: Specifies if host certificates that are requested
                      are allowed to be subdomains of those listed in allowed_domains.
                    type: boolean
                  allowUserCertificates:
                    description: Specifies if certificates are allowed to be signed
                      for use as a 'user'.
                    type: boolean
                  allowUserKeyIds:
                    description: Specifies if users can override the key ID for a
                      signed certificate with the key_id field.
                    type: boolean
                  allowedCriticalOptions:
                    description: Specifies a comma-separated list of critical options
                      that certificates can have when signed.
                    type: string
                  allowedDomains:
                    description: The list of domains for which a client can request
                      a host certificate.
                    type: string
                  allowedDomainsTemplate:
                    description: |-
                      Specifies if allowed_domains can be declared using
                      identity template policies. Non-templated domains are also permitted.
                    type: boolean
                  allowedExtensions:
                    description: Specifies a comma-separated list of extensions that
                      certificates can have when signed.
                    type: string
                  allowedUserKeyConfig:
                    description: |-
                      Set of configuration blocks to define allowed
                      user key configuration, like key type and their lengths. Can be specified multiple times.
                      See
                      Set of allowed public key types and their relevant configuration
                    items:
                      properties:
                        lengths:
                          description: |-
                            A list of allowed key lengths as integers.
                            For key types that do not support setting the length a value of [0] should be used.
                            Setting multiple lengths is only supported on Vault 1.10+. For prior releases length
                            must be set to a single element list.
                            List of allowed key lengths, vault-1.10 and above
                          items:
                            type: number
                          type: array
                        type:
                          description: |-
                            The SSH public key type.
                            Supported key types are:
                            rsa, ecdsa, ec, dsa, ed25519, ssh-rsa, ssh-dss, ssh-ed25519,
                            ecdsa-sha2-nistp256, ecdsa-sha2-nistp384, ecdsa-sha2-nistp521
                            Key type, choices:
                            rsa, ecdsa, ec, dsa, ed25519, ssh-rsa, ssh-dss, ssh-ed25519, ecdsa-sha2-nistp256, ecdsa-sha2-nistp384, ecdsa-sha2-nistp521
                          type: string
                      type: object
                    type: array
                  allowedUsers:
                    description: Specifies a comma-separated list of usernames that
                      are to be allowed, only if certain usernames are to be allowed.
                    type: string
                  allowedUsersTemplate:
                    description: Specifies if allowed_users can be declared using
                      identity template policies. Non-templated users are also permitted.
                    type: boolean
                  backend:
                    description: The path where the SSH secret backend is mounted.
                    type: string
                  backendRef:
                    description: Reference to a Mount in vault to populate backend.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  backendSelector:
                    description: Selector for a Mount in vault to populate backend.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  cidrList:
                    description: The comma-separated string of CIDR blocks for which
                      this role is applicable.
                    type: string
                  defaultCriticalOptions:
                    additionalProperties:
                      type: string
                    description: Specifies a map of critical options that certificates
                      have when signed.
                    type: object
                    x-kubernetes-map-type: granular
                  defaultExtensions:
                    additionalProperties:
                      type: string
                    description: Specifies a map of extensions that certificates have
                      when signed.
                    type: object
                    x-kubernetes-map-type: granular
                  defaultUser:
                    description: Specifies the default username for which a credential
                      will be generated.
                    type: string
                  defaultUserTemplate:
                    description: If set, default_users can be specified using identity
                      template values. A non-templated user is also permitted.
                    type: boolean
                  keyIdFormat:
                    description: Specifies a custom format for the key id of a signed
                      certificate.
                    type: string
                  keyType:
                    description: Specifies the type of credentials generated by this
                      role. This can be either otp, dynamic or ca.
                    type: string
                  maxTtl:
                    description: Specifies the maximum Time To Live value.
                    type: string
                  name:
                    description: |-
                      Specifies the name of the role to create.
                      Unique name for the role.
                    type: string
                  namespace:
                    description: |-
                      The namespace to provision the resource in.
                      The value should not contain leading or trailing forward slashes.
                      The namespace is always relative to the provider's configured namespace.
                      Available only for Vault Enterprise.
                      Target namespace. (requires Enterprise)
                    type: string
                  notBeforeDuration:
                    description: |-
                      Specifies the duration by which to backdate the ValidAfter property.
                      Uses duration format strings.
                      Specifies the duration by which to backdate the ValidAfter property. Uses duration format strings.
                    type: string
                  ttl:
                    description: Specifies the Time To Live value.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.keyType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.keyType)
                || (has(self.initProvider) && has(self.initProvider.keyType))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: SecretBackendRoleStatus defines the observed state of SecretBackendRole.
            properties:
              atProvider:
                properties:
                  algorithmSigner:
                    description: 'When supplied, this value specifies a signing algorithm
                      for the key. Possible values: ssh-rsa, rsa-sha2-256, rsa-sha2-512.'
                    type: string
                  allowBareDomains:
                    description: Specifies if host certificates that are requested
                      are allowed to use the base domains listed in allowed_domains.
                    type: boolean
                  allowHostCertificates:
                    description: Specifies if certificates are allowed to be signed
                      for use as a 'host'.
                    type: boolean
                  allowSubdomains:
                    description: Specifies if host certificates that are requested
                      are allowed to be subdomains of those listed in allowed_domains.
                    type: boolean
                  allowUserCertificates:
                    description: Specifies if certificates are allowed to be signed
                      for use as a 'user'.
                    type: boolean
                  allowUserKeyIds:
                    description: Specifies if users can override the key ID for a
                      signed certificate with the key_id field.
                    type: boolean
                  allowedCriticalOptions:
                    description: Specifies a comma-separated list of critical options
                      that certificates can have when signed.
                    type: string
                  allowedDomains:
                    description: The list of domains for which a client can request
                      a host certificate.
                    type: string
                  allowedDomainsTemplate:
                    description: |-
                      Specifies if allowed_domains can be declared using
                      identity template policies. Non-templated domains are also permitted.
                    type: boolean
                  allowedExtensions:
                    description: Specifies a comma-separated list of extensions that
                      certificates can have when signed.
                    type: string
                  allowedUserKeyConfig:
                    description: |-
                      Set of configuration blocks to define allowed
                      user key configuration, like key type and their lengths. Can be specified multiple times.
                      See
                      Set of allowed public key types and their relevant configuration
                    items:
                      properties:
                        lengths:
                          description: |-
                            A list of allowed key lengths as integers.
                            For key types that do not support setting the length a value of [0] should be used.
                            Setting multiple lengths is only supported on Vault 1.10+. For prior releases length
                            must be set to a single element list.
                            List of allowed key lengths, vault-1.10 and above
                          items:
                            type: number
                          type: array
                        type:
                          description: |-
                            The SSH public key type.
                            Supported key types are:
                            rsa, ecdsa, ec, dsa, ed25519, ssh-rsa, ssh-dss, ssh-ed25519,
                            ecdsa-sha2-nistp256, ecdsa-sha2-nistp384, ecdsa-sha2-nistp521
                            Key type, choices:
                            rsa, ecdsa, ec, dsa, ed25519, ssh-rsa, ssh-dss, ssh-ed25519, ecdsa-sha2-nistp256, ecdsa-sha2-nistp384, ecdsa-sha2-nistp521
                          type: string
                      type: object
                    type: array
                  allowedUsers:
                    description: Specifies a comma-separated list of usernames that
                      are to be allowed, only if certain usernames are to be allowed.
                    type: string
                  allowedUsersTemplate:
                    description: Specifies if allowed_users can be declared using
                      identity template policies. Non-templated users are also permitted.
                    type: boolean
                  backend:
                    description: The path where the SSH secret backend is mounted.
                    type: string
                  cidrList:
                    description: The comma-separated string of CIDR blocks for which
                      this role is applicable.
                    type: string
                  defaultCriticalOptions:
                    additionalProperties:
                      type: string
                    description: Specifies a map of critical options that certificates
                      have when signed.
                    type: object
                    x-kubernetes-map-type: granular
                  defaultExtensions:
                    additionalProperties:
                      type: string
                    description: Specifies a map of extensions that certificates have
                      when signed.
                    type: object
                    x-kubernetes-map-type: granular
                  defaultUser:
                    description: Specifies the default username for which a credential
                      will be generated.
                    type: string
                  defaultUserTemplate:
                    description: If set, default_users can be specified using identity
                      template values. A non-templated user is also permitted.
                    type: boolean
                  id:
                    type: string
                  keyIdFormat:
                    description: Specifies a custom format for the key id of a signed
                      certificate.
                    type: string
                  keyType:
                    description: Specifies the type of credentials generated by this
                      role. This can be either otp, dynamic or ca.
                    type: string
                  maxTtl:
                    description: Specifies the maximum Time To Live value.
                    type: string
                  name:
                    description: |-
                      Specifies the name of the role to create.
                      Unique name for the role.
                    type: string
                  namespace:
                    description: |-
                      The namespace to provision the resource in.
                      The value should not contain leading or trailing forward slashes.
                      The namespace is always relative to the provider's configured namespace.
                      Available only for Vault Enterprise.
                      Target namespace. (requires Enterprise)
                    type: string
                  notBeforeDuration:
                    description: |-
                      Specifies the duration by which to backdate the ValidAfter property.
                      Uses duration format strings.
                      Specifies the duration by which to backdate the ValidAfter property. Uses duration format strings.
                    type: string
                  ttl:
                    description: Specifies the Time To Live value.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
