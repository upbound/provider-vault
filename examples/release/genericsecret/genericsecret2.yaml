apiVersion: generic.vault.upbound.io/v1alpha1
kind: Secret
metadata:
  name: vault-creds-2
spec:
  forProvider:
    path: "generic/vault-creds-2"
    dataJsonSecretRef:
      name: vault-creds
      namespace: vault
      key: credentials
  providerConfigRef:
    name: vault-provider-config
